{
  "name": "healops-test-repo",
  "version": "1.0.0",
  "private": true,
  "description": "Test repository for validating HealOps self-healing CI/CD pipeline",
  "scripts": {
    "backend": "cd apps/backend && npm run start:dev",
    "frontend": "cd apps/frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd apps/backend && npm run build",
    "build:frontend": "cd apps/frontend && npm run build",
    "test": "cd apps/backend && npm test",
    "lint": "cd apps/backend && npm run lint && cd ../frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
